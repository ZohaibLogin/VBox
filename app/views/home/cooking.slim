#pjax-container

	.container-fluid.main-inner-container.no-padding-left
		.row.kh-main-content
			.col-md-12.col-sm-12.col-xs-12.no-padding.cooking-page.most-viewed
				h3.no-margin-bottom Cooking
				.cooking-section-tab.col-md-12.col-sm-12.col-xs-12.no-padding
					.col-md-4.col-sm-4.col-xs-4.no-padding.cooking-latest.active-cooking-tab
						p.text-center Latest
					.col-md-4.col-sm-4.col-xs-4.no-padding.cooking-featured
						p.text-center Featured
					.col-md-4.col-sm-4.col-xs-4.no-padding.cooking-top-rated
						p.text-center Top Rated
				.cooking-latest-inner-content.col-md-12.col-sm-12.col-xs-12
					.col-md-4.col-sm-4.col-xs-12.no-padding.cooking-big-thumb.cooking-section-one
						img.img-responsive.video-thumbnail src="default-video-thumb.png"
						a.video-play-link href="#"
							img src="play.png"
					.col-md-4.col-sm-4.col-xs-12.cooking-sm-thumb.cooking-section-two
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p.no-margin-bottom
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
					.col-md-4.col-sm-4.col-xs-12.cooking-sm-thumb.cooking-section-three
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p 
								span

				.cooking-all-videos-container.col-md-12.col-sm-12.col-xs-12.no-padding.most-viewed.browse-latest
					h4.no-margin Browse All Videos
					.col-md-12.col-sm-12.col-xs-12.browse-all-videos
						- i = 0
						- while i < 24
							- i += 1
							.col-md-2.col-sm-3.col-xs-6.no-padding.cooking-latest-all-video-block
								.video-block.category-block-indent
									= render 'static_video_block'
						.clearfix
						.load-more-wrapper.cooking-load-more-wrapper.load-more-wrapper-latest
							button.load-more-btn.cooking-load-more-btn.load-more-latest Load More 

				.cooking-featured-inner-content.col-md-12.col-sm-12.col-xs-12.no-display
					.col-md-4.col-sm-4.col-xs-12.no-padding.cooking-big-thumb.cooking-section-one
						img.img-responsive.video-thumbnail src="default-video-thumb.png"
						a.video-play-link href="#"
							img src="play.png"
					.col-md-4.col-sm-4.col-xs-12.cooking-sm-thumb.cooking-section-two
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
					.col-md-4.col-sm-4.col-xs-12.cooking-sm-thumb.cooking-section-three
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span

				.cooking-all-videos-container.col-md-12.col-sm-12.col-xs-12.no-padding.most-viewed.browse-featured.no-display
					h4.no-margin Browse All Videos
					.col-md-12.col-sm-12.col-xs-12.browse-all-videos
						- i = 0
						- while i < 24
							- i += 1
							.col-md-2.col-sm-3.col-xs-6.no-padding.cooking-featured-all-video-block
								.video-block
									= render 'static_video_block'
						.clearfix
						.load-more-wrapper.cooking-load-more-wrapper.load-more-wrapper-featured
							button.load-more-btn.cooking-load-more-btn.load-more-featured Load More

				.cooking-top-rated-inner-content.col-md-12.col-sm-12.col-xs-12.no-display
					.col-md-4.col-sm-4.col-xs-12.no-padding.cooking-big-thumb.cooking-section-one
						img.img-responsive.video-thumbnail src="default-video-thumb.png"
						a.video-play-link href="#"
							img src="play.png"
					.col-md-4.col-sm-4.col-xs-12.cooking-sm-thumb.cooking-section-two
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
					.col-md-4.col-sm-4.col-xs-12.cooking-sm-thumb.cooking-section-three
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span 
						.clearfix
						.cooking-sm-container-wrapper.col-md-12.col-sm-12.col-xs-12.no-padding
							.cooking-sm-thumb-wrapper.col-md-4.col-sm-4.col-xs-4.no-padding
								a href="#"
									img.img-responsive src="default-video-thumb.png"
							.cooking-sm-content.col-md-8.col-sm-8.col-xs-8
								p
								span

				.cooking-all-videos-container.col-md-12.col-sm-12.col-xs-12.no-padding.most-viewed.browse-top-rated.no-display
					h4.no-margin Browse All Videos
					.col-md-12.col-sm-12.col-xs-12.browse-all-videos
						- i = 0
						- while i < 24
							- i += 1
							.col-md-2.col-sm-3.col-xs-6.no-padding.cooking-top-rated-all-video-block
								.video-block
									= render 'static_video_block'
						.clearfix
						.load-more-wrapper.cooking-load-more-wrapper.load-more-wrapper-top-rated
							button.load-more-btn.cooking-load-more-btn.load-more-top-rated Load More


javascript:

	$('.video-block').each(function(i) {
			
		var topOfObject = $(this).offset().top,
			bottomOfWindow = $(window).scrollTop() + $(window).height();

		if (bottomOfWindow > topOfObject) {
			$(this).animate({'opacity': '1'}, 500);
		}

	});
    $('.featured-video-block').each(function(i) {
                    
        var topOfObject = $(this).offset().top,
              bottomOfWindow = $(window).scrollTop() + $(window).height();

        if (bottomOfWindow > topOfObject) {
              $(this).animate({'opacity': '1'}, 500);
        }

    });

	$(window).scroll(function() {
		$('.video-block').each(function(i) {
			
			var topOfObject = $(this).offset().top,
				bottomOfWindow = $(window).scrollTop() + $(window).height();

			if (bottomOfWindow > topOfObject) {
				$(this).animate({'opacity': '1'}, 500);
			}

		});
        $('.featured-video-block').each(function(i) {
                    
			var topOfObject = $(this).offset().top,
			    bottomOfWindow = $(window).scrollTop() + $(window).height();

			if (bottomOfWindow > topOfObject) {
			    $(this).animate({'opacity': '1'}, 500);
			}

        });
	});

	$('.cooking-latest').click(function(e) {

		e.preventDefault;

		$(this).siblings().removeClass('active-cooking-tab');
		$(this).addClass('active-cooking-tab');

		if (!($('.browse-featured').hasClass('no-display'))) {
		    console.log('featured');

		    $('.cooking-featured-inner-content').addClass('no-display');
		    $('.browse-featured').addClass('no-display');

		}

		if (!($('.browse-top-rated').hasClass('no-display'))) {
		    console.log('top-rated');

		    $('.cooking-top-rated-inner-content').addClass('no-display');
		    $('.browse-top-rated').addClass('no-display');

		}

		if ($('.browse-latest').hasClass('no-display')) {

		    console.log('latest');
		    $('.cooking-latest-inner-content').removeClass('no-display');
		    $('.browse-latest').removeClass('no-display');

		}

    });

    $('.cooking-featured').click(function(e) {

		e.preventDefault;

		$(this).siblings().removeClass('active-cooking-tab');
		$(this).addClass('active-cooking-tab');

		if (!($('.browse-latest').hasClass('no-display'))) {

		    $('.cooking-latest-inner-content').addClass('no-display');
		    $('.browse-latest').addClass('no-display');

		}

		if (!($('.browse-top-rated').hasClass('no-display'))) {

		    $('.cooking-top-rated-inner-content').addClass('no-display');
		    $('.browse-top-rated').addClass('no-display');

		}

		if ($('.browse-featured').hasClass('no-display')) {

		    $('.cooking-featured-inner-content').removeClass('no-display');
		    $('.browse-featured').removeClass('no-display');

		}

    });

    $('.cooking-top-rated').click(function(e) {

		e.preventDefault;

		$(this).siblings().removeClass('active-cooking-tab');
		$(this).addClass('active-cooking-tab');

		if (!($('.browse-latest').hasClass('no-display'))) {

		    $('.cooking-latest-inner-content').addClass('no-display');
		    $('.browse-latest').addClass('no-display');

		}

		if (!($('.browse-featured').hasClass('no-display'))) {

		    $('.cooking-featured-inner-content').addClass('no-display');
		    $('.browse-featured').addClass('no-display');

		}

		if ($('.browse-top-rated').hasClass('no-display')) {

		    $('.cooking-top-rated-inner-content').removeClass('no-display');
		    $('.browse-top-rated').removeClass('no-display');

		}

    });

    // Fetching more cooking video results on load more click

    var   pageCounter = 1,
          htmlRowEl,
          dynamicPartOne,
          dynamicPartThree,
          dynamicPartFour,
          dynamicPartFive,
          dynamicPartSix,
          dynamicParts,
          outerColDiv = '<div class="col-md-2 col-sm-3 col-xs-6 no-padding">',
          videoBlockDiv = '<div class="video-block">',
          titleDiv = '<div class="vblock-content-wrapper">',
          twoClosingDivs = '</div></div>';

    $('.load-more-latest').click(function(e) {

		e.preventDefault;

		pageCounter++;

		var loadMoreDataToSend = { page: pageCounter, category: 'cooking_latest' };
		var rowInitial = outerColDiv + videoBlockDiv;

		bp_ajax({

		    action: 'more_results',
		    method: 'get',
		    contentType: 'application/json',
		    data: loadMoreDataToSend,
		    notifications: false,
		    spinner: true,
		    callback: function(data) {

				if (data['data']['current_page'] == data['data']['total_pages']) {
				    $(this).hide();
				}

				for (var i=0; i< data['data']['data'].length; i++) {

				    dynamicPartOne = '<img class="img-responsive video-thumbnail" src=' + data['data']['data'][i]['thumb_url'] + '></img>';
				    dynamicPartThree = '<a class="video-play-link" href="/video?id=' + data['data']['data'][i]['id'] + '"> <img src="play.png"></img> </a>';
				    dynamicPartFour = '<p>' + data['data']['data'][i]['title'] + '</p>';
				    dynamicPartFive = '<span>' + data['data']['data'][i]['views'] + ' views </span>';
				    dynamicPartSix = '<div class="clearfix"></div> <span class="flaticon-clock96">' + data['data']['data'][i]['duration'] + '</span>';

				    dynamicPartsOne = dynamicPartThree;
				    dynamicPartsTwo = titleDiv + dynamicPartFour + dynamicPartFive + dynamicPartSix;                      

				    htmlRowEl = rowInitial + dynamicPartOne + dynamicPartsOne + dynamicPartsTwo + twoClosingDivs;

				    $('.load-more-wrapper-latest').before(htmlRowEl);

				}

				$('.video-block').each(function(i) {
			
					var topOfObject = $(this).offset().top,
						bottomOfWindow = $(window).scrollTop() + $(window).height();

					if (bottomOfWindow > topOfObject) {
						$(this).animate({'opacity': '1'}, 500);
					}

				});

		    }   

		});

    });

	// Fetching more cooking video results on load more click

    var   pageCounter = 1,
          htmlRowEl,
          dynamicPartOne,
          dynamicPartThree,
          dynamicPartFour,
          dynamicPartFive,
          dynamicPartSix,
          dynamicParts,
          outerColDiv = '<div class="col-md-2 col-sm-3 col-xs-6 no-padding">',
          videoBlockDiv = '<div class="video-block">',
          titleDiv = '<div class="vblock-content-wrapper">',
          twoClosingDivs = '</div></div>';

    $('.load-more-featured').click(function(e) {

		e.preventDefault;

		pageCounter++;

		var loadMoreDataToSend = { page: pageCounter, category: 'cooking_featured' };
		var rowInitial = outerColDiv + videoBlockDiv;

		bp_ajax({

		    action: 'more_results',
		    method: 'get',
		    contentType: 'application/json',
		    data: loadMoreDataToSend,
		    notifications: false,
		    spinner: true,
		    callback: function(data) {

				if (data['data']['current_page'] == data['data']['total_pages']) {
				    $(this).hide();
				}

				for (var i=0; i< data['data']['data'].length; i++) {

				    dynamicPartOne = '<img class="img-responsive video-thumbnail" src=' + data['data']['data'][i]['thumb_url'] + '></img>';
				    dynamicPartThree = '<a class="video-play-link" href="/video?id=' + data['data']['data'][i]['id'] + '"> <img src="play.png"></img> </a>';
				    dynamicPartFour = '<p>' + data['data']['data'][i]['title'] + '</p>';
				    dynamicPartFive = '<span>' + data['data']['data'][i]['views'] + ' views </span>';
				    dynamicPartSix = '<div class="clearfix"></div> <span class="flaticon-clock96">' + data['data']['data'][i]['duration'] + '</span>';

				    dynamicPartsOne = dynamicPartThree;
				    dynamicPartsTwo = titleDiv + dynamicPartFour + dynamicPartFive + dynamicPartSix;                      

				    htmlRowEl = rowInitial + dynamicPartOne + dynamicPartsOne + dynamicPartsTwo + twoClosingDivs;

				    $('.load-more-wrapper-latest').before(htmlRowEl);

				}

				$('.video-block').each(function(i) {
			
					var topOfObject = $(this).offset().top,
						bottomOfWindow = $(window).scrollTop() + $(window).height();

					if (bottomOfWindow > topOfObject) {
						$(this).animate({'opacity': '1'}, 500);
					}

				});

		    }   

		});

    });

	// Fetching more cooking top rated video results on load more click

    var   pageCounter = 1,
          htmlRowEl,
          dynamicPartOne,
          dynamicPartThree,
          dynamicPartFour,
          dynamicPartFive,
          dynamicPartSix,
          dynamicParts,
          outerColDiv = '<div class="col-md-2 col-sm-3 col-xs-6 no-padding">',
          videoBlockDiv = '<div class="video-block">',
          titleDiv = '<div class="vblock-content-wrapper">',
          twoClosingDivs = '</div></div>';

    $('.load-more-top-rated').click(function(e) {

		e.preventDefault;

		pageCounter++;

		var loadMoreDataToSend = { page: pageCounter, category: 'cooking_top_rated' };
		var rowInitial = outerColDiv + videoBlockDiv;

		bp_ajax({

		    action: 'more_results',
		    method: 'get',
		    contentType: 'application/json',
		    data: loadMoreDataToSend,
		    notifications: false,
		    spinner: true,
		    callback: function(data) {

				if (data['data']['current_page'] == data['data']['total_pages']) {
				    $(this).hide();
				}

				for (var i=0; i< data['data']['data'].length; i++) {

				    dynamicPartOne = '<img class="img-responsive video-thumbnail" src=' + data['data']['data'][i]['thumb_url'] + '></img>';
				    dynamicPartThree = '<a class="video-play-link" href="/video?id=' + data['data']['data'][i]['id'] + '"> <img src="play.png"></img> </a>';
				    dynamicPartFour = '<p>' + data['data']['data'][i]['title'] + '</p>';
				    dynamicPartFive = '<span>' + data['data']['data'][i]['views'] + ' views </span>';
				    dynamicPartSix = '<div class="clearfix"></div> <span class="flaticon-clock96">' + data['data']['data'][i]['duration'] + '</span>';

				    dynamicPartsOne = overlayDiv + dynamicPartThree;
				    dynamicPartsTwo = titleDiv + dynamicPartFour + dynamicPartFive + dynamicPartSix;                      

				    htmlRowEl = rowInitial + dynamicPartOne + dynamicPartsOne + dynamicPartsTwo + twoClosingDivs;

				    $('.load-more-wrapper-top-rated').before(htmlRowEl);

				}

				$('.video-block').each(function(i) {
			
					var topOfObject = $(this).offset().top,
						bottomOfWindow = $(window).scrollTop() + $(window).height();

					if (bottomOfWindow > topOfObject) {
						$(this).animate({'opacity': '1'}, 500);
					}

				});

		    }   

		});

    });

	$(window).load(function() {

		$('.cooking-menu').addClass('active').siblings().removeClass('active');

		// Latest

        bp_ajax({
    
			action: 'cooking_latest',
			method: 'get',
			contentType: 'application/json',
			notifications: false,
			spinner: true,
			callback: function(data) {

			    if (data['data']['total_pages'] == 1) {
			        $('.load-more-latest').hide();
			    }

			    //big image
			    $('.cooking-latest-inner-content .cooking-section-one a').attr("href", "/video?id=" + data['data']['data'][0]['id']);
			    $('.cooking-latest-inner-content .cooking-section-one img.video-thumbnail').attr("src", data['data']['data'][0]['thumb_url']);
			    
			    //small image and content
			    var j = 0;
			    $('.cooking-latest-inner-content .cooking-section-two .cooking-sm-container-wrapper').each(function(i, obj) {
					j= i+1;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span").text(data['data']['data'][j]['views'] + " views");
			    });
			    $('.cooking-latest-inner-content .cooking-section-three .cooking-sm-container-wrapper').each(function(i, obj) {
					j= i+4;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span").text(data['data']['data'][j]['views'] + " views");
			    });
			    $('.browse-latest .cooking-latest-all-video-block').each(function(i, obj) {
					j= i+7;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img.video-thumbnail").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span.view-count").text(data['data']['data'][j]['views'] + " views");
					$(this).find("span.duration").addClass("flaticon-clock96").text(data['data']['data'][j]['duration']);
			    });
			}

        });

		// Featured

        bp_ajax({
    
			action: 'cooking_featured',
			method: 'get',
			contentType: 'application/json',
			notifications: false,
			spinner: true,
			callback: function(data) {

			    if (data['data']['total_pages'] == 1) {
			        $('.load-more-featured').hide();
			    }

			    //big image
			    $('.cooking-featured-inner-content .cooking-section-one a').attr("href", "/video?id=" + data['data']['data'][0]['id']);
			    $('.cooking-featured-inner-content .cooking-section-one img.video-thumbnail').attr("src", data['data']['data'][0]['thumb_url']);
			    
			    //small image and content
			    var j = 0;
			    $('.cooking-featured-inner-content .cooking-section-two .cooking-sm-container-wrapper').each(function(i, obj) {
					j= i+1;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span").text(data['data']['data'][j]['views'] + " views");
			    });
			    $('.cooking-featured-inner-content .cooking-section-three .cooking-sm-container-wrapper').each(function(i, obj) {
					j= i+4;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span").text(data['data']['data'][j]['views'] + " views");
			    });
			    $('.browse-featured .cooking-featured-all-video-block').each(function(i, obj) {
					j= i+7;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img.video-thumbnail").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span.view-count").text(data['data']['data'][j]['views'] + " views");
					$(this).find("span.duration").addClass("flaticon-clock96").text(data['data']['data'][j]['duration']);
			    });
			}

        });

		// Top Rated

        bp_ajax({
    
			action: 'cooking_top_rated',
			method: 'get',
			contentType: 'application/json',
			notifications: false,
			spinner: true,
			callback: function(data) {

			    if (data['data']['total_pages'] == 1) {
			        $('.load-more-top-rated').hide();
			    }

			    //big image
			    $('.cooking-top-rated-inner-content .cooking-section-one a').attr("href", "/video?id=" + data['data']['data'][0]['id']);
			    $('.cooking-top-rated-inner-content .cooking-section-one img.video-thumbnail').attr("src", data['data']['data'][0]['thumb_url']);
			    
			    //small image and content
			    var j = 0;
			    $('.cooking-top-rated-inner-content .cooking-section-two .cooking-sm-container-wrapper').each(function(i, obj) {
					j= i+1;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span").text(data['data']['data'][j]['views'] + " views");
			    });
			    $('.cooking-top-rated-inner-content .cooking-section-three .cooking-sm-container-wrapper').each(function(i, obj) {
					j= i+4;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span").text(data['data']['data'][j]['views'] + " views");
			    });
			    $('.browse-top-rated .cooking-top-rated-all-video-block').each(function(i, obj) {
					j= i+7;
					$(this).find("a").attr("href", "/video?id=" + data['data']['data'][j]['id']);
					$(this).find("img.video-thumbnail").attr("src", data['data']['data'][j]['thumb_url']);
					$(this).find("p").text(data['data']['data'][j]['title']);
					$(this).find("span.view-count").text(data['data']['data'][j]['views'] + " views");
					$(this).find("span.duration").addClass("flaticon-clock96").text(data['data']['data'][j]['duration']);
			    });

			}

        });

	});